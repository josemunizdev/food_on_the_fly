name: fast
type: xgboost
# Model hyperparameters
parameters:
  n_estimators: 200
  max_depth: 3
  learning_rate: 0.05
  subsample: 0.7
  colsample_bytree: 0.7
  gamma: 0.1
  min_child_weight: 3
  random_state: 42
  n_jobs: -1
