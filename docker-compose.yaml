version: '3.8'

services:
  food_on_the_fly:
    build:
      context: .
      dockerfile: dockerfiles/Dockerfile
    container_name: food_on_the_fly_app
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8000:8000"
